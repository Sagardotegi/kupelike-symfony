{% extends 'base.html.twig' %}

{% block body %}
<div class="container-fluid">
    
    <div class="row">
        <div class="col-xs-10 col-xs-offset-1">
            <div class="row">
                <h1 class="mtitle title-padding text-center">Editar usuario</h1>
            </div>
        
            <div class="row">
                <form action="{{path('actualizar_usuario', { id: usuario.id })}}" class="contact_form">
                    <input type="text" name="nombre" class="formulario" value="{{ usuario.nombre }}" />
                    <input type="text" name="apellidos" class="formulario" value="{{ usuario.apellidos }}" /> 
                    <select name="sidreria" class="formulario">
                        <option value="{{ usuario.idSidreria }}-{{ usuario.nombreSidreria }}">{{ usuario.nombreSidreria }}</option>
                        {% for sagardotegi in sagardotegis %}
                            <option value="{{ sagardotegi.id }}-{{ sagardotegi.nombre }}">{{ sagardotegi.nombre }}</option>
                        {% endfor %}
                    </select>
                    <input type="number" name="telefono" class="formulario" value="{{ usuario.telefono }}" /> 
                    <input type="email" name="email" class="formulario" value="{{ usuario.email }}" /> 
                    <input type="text" name="username" class="formulario" value="{{ usuario.username }}" />
                    <button type="submit" class= "formulario" id="enviar" >Actualizar</button>
                </form>
            </div>
        </div>
    </div>
    
    
    

{% endblock %}