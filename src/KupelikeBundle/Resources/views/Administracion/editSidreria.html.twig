{% extends 'base.html.twig' %}

{% block body %}
<div class="container-fluid">
    
    <div class="row-editarSidreria" style="margin-top:50px;">
         <form action="{{ path("actualizar_sagardotegi", {id: sidreria.id})}}">
             <input type="text" name="nombre" value="{{sidreria.nombre}}">
             <input type="text" name="descripcion" value="{{sidreria.descripcion}}">
             <button type="submit" class="btn btn-default">Guardar</button>
         </form>
        
         <a href="{{ path("administracion_usuarios",{nombreSidreria: "Petritegi"})}}"><button class="btn btn-default">Cancelar</button></a>
    </div>
     
    
    

{% endblock %}