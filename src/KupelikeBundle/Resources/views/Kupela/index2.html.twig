{% extends 'base.html.twig' %}

{% block body %}
   <div class="container">
   
        <div class="row single-sagardotegi">
            <div class="col-xs-6">
                <h2 class="card-title text-uppercase">{{ sagardotegi.nombre }}</h2>
                    <p class="single-description" id="descripcion">{{ sagardotegi.descripcion }}</p>
                    <a href="{{ path("sagardotegi_mapa", {idSagardotegi:sagardotegi.id}) }}"><button type="button" class="btn btn-default">Como llegar</button></a>
                    <a href="http://www.petritegi.com/es/contacto"><button type="button" class="btn btn-default">Contacto</button></a>
            </div>
            <div class="col-xs-6">
                <div class="card-single-direction text-uppercase text-center"><h4>{{ sagardotegi.direccion }}</h4></div>
                <img class="single-img img-responsive img-rounded" src="{{asset(sagardotegi.foto)}}"></img>
            </div>
        </div>
    </div>
    
    <div class="container">
        
    <div class="row p-x-1 kupela-list">
        <div class="col-xs-offset-1 col-xs-10">
    {% for kupela in kupelas %}
        
      
        <div class="row single-kupela">
            <div class="col-xs-6">
                <div class="caption">
                    <h3 class="card-title text-uppercase">{{kupela.nombre}}</h3>
                    <p class="single-description">{{kupela.descripcion}}</p>
                </div>
            </div>
            <div class="col-xs-4">
                <img src="{{asset(kupela.foto)}}" alt="kupela" class="single-kupela-img img-circle img-responsive pull-right">
            </div>
            <div class="col-xs-2 votos">
                <div class="row">
                    <div class="col-xs-12">
                        <h3>{{kupela.numVotos}}</h3>
                    </div>
                    <div class="col-xs-12">
                        <div class="fb-like" id="1710432339217714" data-href="https://www.facebook.com/kupelike" data-layout="standard" data-action="like" data-size="large" data-show-faces="false" data-share="false"></div>
					    <!--<a href="aa" id="1710432339217714"></a>-->
					    <button class="vote-button">
					    <img class="like-kupela img-responsive" src= "/web/img/logo_reverse.jpg" onclick="likeado()">
					    </button>
                    </div>
                </div>
            </div>
        </div>
      
    {% endfor %}
        </div>
    </div>
{% endblock %} 